# Database URL (Self-hosted PostgreSQL)
# Local development (with docker-compose.dev.yml):
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/reports_app_dev
# Production:
# DATABASE_URL=postgresql://user:password@your-db-host:5432/reports_app

# Airtable - DeBloq Base (Read for dashboard)
AIRTABLE_API_KEY=your-airtable-api-key
AIRTABLE_BASE_ID_DEBLOQ=your-debloq-base-id

# Airtable - Reports Base (Write via n8n)
AIRTABLE_BASE_ID_REPORTS=your-reports-base-id

# Authentication (Magic Link with JWT)
JWT_SECRET=your-super-secret-key-at-least-32-characters-long-change-this
RESEND_API_KEY=re_your_resend_api_key

# n8n Webhook
N8N_WEBHOOK_URL=https://n8n.example.com/webhook/complete-work-order
N8N_WEBHOOK_SECRET=your-webhook-secret

# AWS S3 (Day 2+)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1
AWS_S3_BUCKET=zite-technician-photos

# Application
NEXT_PUBLIC_APP_URL=https://reports.bloqit.io

# Admin Users (comma-separated list of emails)
# These users will have access to admin routes like /admin/cleanup
ADMIN_EMAILS=admin@example.com,manager@example.com
